# Wagent Skills

Each skill lives at `skills/<name>/SKILL.md`. Skills are provider-neutral instruction bundles intended for both humans and coding/agent runtimes.

Use YAML-style frontmatter keys `name`, `description`, and comma-separated `tags`, followed by Markdown instructions. See `concise/SKILL.md`.
